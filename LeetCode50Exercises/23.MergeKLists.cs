using System;
namespace LeetCode50Exercises
{
    public class MergeLists
    {
        /*
         * Merge K sorted lists and return it as one sorted lists
         * O(Log(n))
        */

        private static Comparator<LinkedListNode> comparator = new 

        public LinkedListNode<int> mergeKSortedLists(LinkedListNode<int> lists)
        {
        }
    }
}

